---
sidebar_position: 1
---

# 2026-04-05

- `enable secret` を忘れた（`line con 0 password` と混同）
  - `line con 0 password`: コンソールログイン用
  - `enable secret`: 特権モード(#)昇格用、MD5暗号化
- GNS3 c7200 は `line con 0` に `privilege level 15` がデフォルトで入っており、`enable` なしで特権に入れる（本番環境とは異なる）
- インタフェースをupにしたが `ip address` を忘れた
